<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.lab1024.sa.admin.module.business.wecom.dao.WecomMessageMediaDao">

    <!-- 查询待下载的媒体文件 @author wangxiao -->
    <select id="queryPendingDownload" resultType="net.lab1024.sa.admin.module.business.wecom.domain.entity.WecomMessageMediaEntity">
        SELECT
            media_id,
            message_id,
            sdk_file_id,
            media_type,
            file_name,
            file_size,
            retry_count
        FROM t_wecom_message_media
        WHERE download_status = 0
           OR (download_status = 3 AND retry_count &lt; 3)
        ORDER BY create_time ASC
        LIMIT #{limit}
    </select>

    <!-- 更新下载状态 @author wangxiao -->
    <update id="updateDownloadStatus">
        UPDATE t_wecom_message_media
        SET download_status = #{downloadStatus},
            update_time = NOW()
            <if test="fileId != null">
                , file_id = #{fileId}
                , download_time = NOW()
            </if>
            <if test="failReason != null and failReason != ''">
                , fail_reason = #{failReason}
                , retry_count = retry_count + 1
            </if>
        WHERE media_id = #{mediaId}
    </update>

</mapper>
